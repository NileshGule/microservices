receivers:
  otlp:
    protocols:
      grpc:
      http:
        cors:
          allowed_origins:
            - "http://*"
            - "https://*"

################# Wildcard Option 1 ################
#          allowed_origins: [ "*" ]

################# Individual services ################
#          - https://localhost:8080
#          - https://localhost:3000
#          - http://localhost:3000
##          allowed_headers: [ "*" ]

processors:
  batch:

exporters:
  logging:
    loglevel: debug
  jaeger:
    endpoint: jaeger-service:14250
    tls:
      insecure: true

#  otlp:
#    endpoint: "jaeger:4317"
#    tls:
#      insecure: true
#    prometheus:
#      endpoint: "otelcol:9464"
#      resource_to_telemetry_conversion:
#        enabled: true
#      enable_open_metrics: true


#
#processors:
#  batch:
#  filter:
#    metrics:
#      exclude:
#        match_type: strict
#        metric_names:
#          - queueSize
#
#connectors:
#  spanmetrics:

service:
  pipelines:
    traces:
      receivers:  [ otlp ]
      processors: [ batch ]
      exporters:  [ logging, jaeger ] # [otlp, logging, spanmetrics]
    metrics:
      receivers: [ otlp ] # spanmetrics
#      processors: [ filter, batch ]
      exporters: [ logging ] # [prometheus, logging]
    logs:
      receivers: [ otlp ]
      processors: [ batch ]
      exporters: [ logging ]